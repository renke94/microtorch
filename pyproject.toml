[project]
name = "microtorch"
version = "0.1.0"
description = "A minimal PyTorch clone for educational purposes using plain NumPy"
readme = "README.md"
authors = [

]
requires-python = ">=3.12"

# https://docs.astral.sh/uv/concepts/projects/dependencies/#adding-dependencies
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.120.4",
    "numpy>=2.3.4",
    "pydantic>=2.12.3",
    "python-multipart>=0.0.20",
    "torch>=2.9.0",
    "tqdm>=4.67.1",
    "uvicorn>=0.38.0",
]

[project.scripts]
main = "microtorch:main"
gpt = "tests.mha_test:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/microtorch", "src/tests"]


[dependency-groups]
lint = [
    "pyright>=1.1.407",
    "ruff>=0.14.3",
]

dev = [
    "black>=25.9.0",
    "lefthook>=2.0.2",
    "pytest>=8.4.2",
]

docs = []


[tool.ruff]
line-length = 120

[tool.ruff.lint]
extend-select = [
    "Q",
    "RUF100",
    "C90",
    "UP",
    "I",
    "D",
    "F",
    "E",
    "W",
    "N",
    "ANN",
    "ASYNC",
    "S",
]
flake8-quotes = { inline-quotes = "single", multiline-quotes = "double" }
isort = { combine-as-imports = true, known-first-party = ["microtorch"] }
mccabe = { max-complexity = 15 }
ignore = [
    "D100", # ignore missing docstring in module
    "D102", # ignore missing docstring in public method
    "D104", # ignore missing docstring in public package
    "D105", # ignore missing docstring in magic methods
    "D107", # ignore missing docstring in __init__ methods
    "S101", # allow assert statements
    "N806", # ignore uppercase variable names
    "UP015", # ignore explicit file read mode in open statements
    "E731", # ignore lambda functions outside of def statements
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.format]
quote-style = 'single'

[tool.pyright]
typeCheckingMode = "strict"
reportPrivateUsage = false
reportMissingTypeStubs = false
reportUnnecessaryIsInstance = false
reportUnnecessaryTypeIgnoreComment = true
include = ["src"]
venvPath = ".venv"
